server:
  port: 8080

spring:
  datasource:
  url : jdbc:h2:mem:aviationDB
  driverClassName: org.h2.Driver
  username: sa
  password:
  jpa:
    generate-ddl: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create